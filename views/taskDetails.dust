{>"layout" /}
{<content}
  <h1>{task.name}</h1>
  <div class="container">
    <form action="/api/task" method="POST">
      <input type="hidden" name="_csrf" value="{csrf}" />
      <div class="form-group row control-group">
        <label for="example-text-input" class="col-sm-2 col-form-label">Name</label>
        <div class="col-sm-5 controls">
        <input class="form-control" type="text" name="schedule" placeholder="" value="{task.name}" {#task.name}readonly{/task.name}/>
        </div>
      </div>
      <div class="form-group row control-group">
        <label for="example-text-input" class="col-sm-2 col-form-label">Schedule</label>
        <div class="col-sm-5 controls">
          <input class="form-control" type="text" name="schedule" placeholder="*/12 * * * *" value="{task.schedule}" />
        </div>
      </div>
      <button type="submit" class="btn btn-primary">Save</button>
    </form>
  </div>
  <table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>Started</th>
      <th>Ended</th>
      <th>Failed</th>
    </tr>
  </thead>
  <tbody>
    {#task.jobs}
    <tr onClick="showLogs()">
      <td>{id}</td>
      <td>{@formatDate date="{startedDate}" format="YYYY/MM/DD HH:mm" /}</td>
      <td>{@formatDate date="{endedDate}" format="YYYY/MM/DD HH:mm" /}</td>
      <td>{@formatBoolean value="{failed}" /}</td>
    </tr>
    <tr>
      <td>
      <code>{#logs}{loggedDate}: {message}<br/>{/logs}</code>
      </td>
    </tr>
    {/task.jobs}
  </tbody>
  </table>
<form>
<script>
  function showLogs(id) {

  }
</script>
{/content}